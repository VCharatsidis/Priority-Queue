# Priority-Queue
Priority Queue example in C++
This priority Queue takes sudoku Boxes and returns the most constraint one.
Every box has a binary long that  represents the available values.
The comparator compares the binary longs with bit wise opperations.
